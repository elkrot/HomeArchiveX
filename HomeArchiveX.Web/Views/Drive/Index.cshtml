@model HomeArchiveX.Web.Models.DriveViewModel 

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Ключ
        </th>
        <th>
           Наименование
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.DriveItems) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DriveId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.DriveId  }) |
            @Html.ActionLink("Details", "Detail", new { id = item.DriveId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.DriveId })
        </td>
    </tr>
}
    @Html.Partial("_Paginator",Model.PaginatorViewModel)

</table>
