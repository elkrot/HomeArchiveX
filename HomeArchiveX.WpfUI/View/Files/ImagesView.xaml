<UserControl x:Class="HomeArchiveX.WpfUI.View.Files.ImagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomeArchiveX.WpfUI.View.Files"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Honeydew">

        <ListBox ItemsSource="{Binding Path=ImageToEntities}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="5" Rows="6" />
                    </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="100" Height="100"  
              Margin="20,20,0,0">
                        <StackPanel>
                            <TextBlock Text="{Binding Path=Image.ThumbnailPath}"/>
                            <Image Source="{Binding Path=Image.ThumbnailPath}" Width="60" Height="60"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>

            </ListBox.ItemTemplate>
        </ListBox>


        <!--<ListView Name="List" ItemsSource="{Binding Path=ImageToEntities}"  VerticalContentAlignment="Top" Height="500" 
          ScrollViewer.VerticalScrollBarVisibility="Visible"
          ScrollViewer.CanContentScroll="False"                  
          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                           >

               <ListView.View>
                
                <GridView>
                    -->
        <!--<GridViewColumn DisplayMemberBinding="{Binding Path=Image.ThumbnailPath}" Width="300">Название</GridViewColumn>-->
        <!--
                    <GridViewColumn Width="300" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Path=Image.ThumbnailPath}"/>
                                    <Image Source="{Binding Path=Image.ThumbnailPath}" Width="60" Height="60"/>
                                </StackPanel>
                                
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    -->
        <!--<Image Source="Images/Blue hills.jpg"></Image>-->
        <!--
                </GridView>
            </ListView.View>

        </ListView>-->
    </Grid>
</UserControl>
