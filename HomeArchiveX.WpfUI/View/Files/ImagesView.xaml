<UserControl x:Class="HomeArchiveX.WpfUI.View.Files.ImagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomeArchiveX.WpfUI.View.Files"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Honeydew">

       
         
        <ListBox ItemsSource="{Binding Path=ImageToEntities}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                            <!--<Image Source="{Binding Path=Image.ThumbnailPath}" Width="80" Height="80"/>-->
                    <MediaElement LoadedBehavior="Play" Source="{Binding Path=Image.ThumbnailPath}" Width="80" Height="80"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>           
            
    


    </Grid>
</UserControl>
