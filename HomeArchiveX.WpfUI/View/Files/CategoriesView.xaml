<UserControl x:Class="HomeArchiveX.WpfUI.View.Files.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomeArchiveX.WpfUI.View.Files"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="MediumVioletRed">
        <Button Content="+" Command="{Binding CategoryAddCommand}"/>
        <ListBox ><!--ItemsSource="{Binding Path=CategoryToEntities}"-->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock  /><!--Text="{Binding Path=Category.Title}"-->
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
